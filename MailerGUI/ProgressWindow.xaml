<Window x:Class="MailerGUI.ProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MailerGUI"
        mc:Ignorable="d"
        Title="ProgressWindow" Height="120" Width="360" ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Mailing..." VerticalAlignment="Center" HorizontalAlignment="Left" Margin="7,0,0,0" FontSize="16"/>
        <ProgressBar x:Name="progbar" Grid.Row="1" VerticalAlignment="Center" Height="30" Margin="18"/>
    </Grid>
</Window>
